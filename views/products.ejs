<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Airline</title>
    <style>
        *{
            margin:0;
            padding: 0;
        }
        body{
            background-color: darksalmon;
            color: #555;
            font-family: sans-serif;
            background-color: darkgoldenrod;
        }
        .container{
            margin-top: 100px;
            display: flex;
            justify-content: center;
        }
        .card{
            width: 200px;
            height: 140px;
            background: #fff;
            margin:0 20px;
        }
        span{
            font-size: 1.3rem;
            color: indigo;
            margin:0 10px;
        }
        .submit{
            border:none;
            outline:none;
            display: block;
            margin:auto;
            width: 80px;
            height:40px;
            background-color: red;
            color:#fff;
            padding: 10px;
        }
    </style>
</head>
<body>
    <div class="container">
        <% docs.forEach(doc=>{ %> 
        <form action="/delete/<%= doc._id %>" method="post">
        <div class="card">
            <div>
                <span>Name:</span><span><%= doc.name %> </span>
            </div>
            <div>
                <span>Age:</span><span><%= doc.age %> </span>
            </div>
            <div>
                <span>Arrival:</span><span><%= doc.arr %> </span>
            </div>
            <div>
                <span>Departure:</span><span><%= doc.dep %></span>
            </div>
            <button type="submit" class="submit">Delete</button>
        </div>
        </form>
    <% }) %> 
    </div>
</body>
</html>